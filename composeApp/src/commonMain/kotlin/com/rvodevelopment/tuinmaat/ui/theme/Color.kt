package com.rvodevelopment.tuinmaat.ui.theme

import androidx.compose.ui.graphics.Color

// --- Basis Kleurenpalet ---
val GrasGroen = Color(0xFF7BA65D)    // Accentkleur voor acties
val BladGroen = Color(0xFF81C784)    // Zachtere groentint voor variatie
val ZachtBeige = Color(0xFFE8E8E0)   // De rustige achtergrondkleur (iets donkerder gemaakt)
val OrganischGroen = Color(0xFFE5EEE5) // Heel licht, bijna wit-groen (iets donkerder gemaakt)
val AchtergrondGroenLicht = Color(0xFFD6E9D7) // Heel zacht groen (bovenaan) (iets donkerder gemaakt)
val AchtergrondGroenMidden = Color(0xFFB9DBBB) // Iets dieper groen (onderaan) (iets donkerder gemaakt)
val DonkerGroen = Color(0xFF2E7D32) // Voor je teksten

// --- Neumorphic Kleuren (Cruciaal voor het diepte-effect) ---
val ShadowLight = Color(0xFFFFFFFF)  // Puur wit voor de lichtinval (linksboven)
val ShadowDark = Color(0xFFD1D1C8)   // Net iets donkerder dan ZachtBeige voor de schaduw (rechtsonder) (aangepast op nieuwe basiskleur)

// --- Extra Details ---
val GlassyWhite = Color(0xFFFFFFFF).copy(alpha = 0.6f)
